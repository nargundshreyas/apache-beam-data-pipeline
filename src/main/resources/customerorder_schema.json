{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Product",
  "description": "A product from the catalog",
  "type": "object",
  "properties": {
    "orderId": {
      "description": "The unique identifier for an order",
      "type": "string"
    },
    "orderDetails": {
      "description": "List of orders in customer order object",
      "type": "array"
    },
    "customer": {
      "type": "object",
      "description": "Details of customer placing the order",
      "properties": {
        "customerId": {
          "type": "string",
          "description": "The unique identifier for a customer"
        },
        "name": {
          "type": "string",
          "description": "The name of the customer"
        },
        "address": {
          "type": "object",
          "description": "Address of the customer",
          "properties": {
            "houseNo": {
              "type": "string",
              "description": "House no of in the address of customer"
            },
            "street": {
              "type": "string",
              "description": "street name in the address of customer"
            },
            "city": {
              "type": "string",
              "description": "city in the address of customer"
            },
            "postalCode": {
              "type": "string",
              "description": "postal code in the address of customer",
              "minLength": 1
            }
          },
          "required": [
            "postalCode"
          ]
        }
      },
      "required": [
        "customerId"
      ]
    },
    "time" : {
      "type": "string",
      "description": "Timestamp at which the object is created in the system"
    }
  },
  "required": [
    "orderId",
    "customer"
  ]
}